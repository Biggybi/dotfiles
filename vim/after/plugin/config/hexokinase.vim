if ! exists(':HexokinaseToggle')
  finish
endif

let g:Hexokinase_highlighters = [
      \   'virtual',
      \   'background',
      \   'backgroundfull',
      \   'sign_column',
      \   'foreground',
      \   'foregroundfull'
      \ ]
