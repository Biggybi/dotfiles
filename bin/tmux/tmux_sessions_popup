#!/bin/bash

session=$(tmux list-sessions\
	| sed -E 's/:.*#//'\
	| grep -v \"^$(tmux display-message -p '#S')\$\"\
	| fzf --reverse\
	| sed 's/:.*//')
[[ $session != '' ]] && tmux switch-client -t $session
